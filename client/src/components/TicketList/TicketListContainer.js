import React, { Component } from "react";
import TicketList from "./TicketList";

export default class TicketListContainer extends Component {
  render() {
    return (
      <div>
        <TicketList />
      </div>
    );
  }
}
